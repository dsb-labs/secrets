package view

import "github.com/davidsbond/keeper/internal/server/ui/layout";

type (
    DashboardViewModel struct {
    
    }
)

templ Dashboard(model DashboardViewModel) {
   @layout.Main("Dashboard", dashboard(model))
}

templ dashboard(model DashboardViewModel) {
 <div>test</div>
}
