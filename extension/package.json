{
  "name": "@keeper/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "zip": "vite build && cd dist && zip -r keeper.zip . --exclude keeper.zip",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "preact": "^10.29.1",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  }
}
