name: release

on:
  push:
    tags:
      - "v*"

permissions:
  packages: write
  contents: write

concurrency:
  group: ${{ github.workflow }}/${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  server:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Install go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker login
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ github.token }}

      - name: Publish release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        uses: goreleaser/goreleaser-action@v7
        with:
          args: release --clean

  extension:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Install pnpm
        uses: pnpm/action-setup@v5

      - name: Install NodeJS
        uses: actions/setup-node@v6
        with:
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Build extension
        run: |
          pnpm install --frozen-lockfile
          cd extension && pnpm version ${GITHUB_REF_NAME#v} --no-git-tag-version && cd ..
          pnpm zip:extension
          mv extension/dist/secrets.zip extension/dist/extension_${GITHUB_REF_NAME#v}.zip

      - name: Upload extension to release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: gh release upload ${{ github.ref_name }} extension/dist/extension_${GITHUB_REF_NAME#v}.zip
